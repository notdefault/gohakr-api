'use strict'

var config = 
    { app: 
        { domain: "gohacker.org"
        , port: 11111
        }
    , mongodb:
        { host: 'localhost'
        , database: 'test'
        }
    }

module.exports = config
