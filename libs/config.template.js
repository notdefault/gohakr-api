'use strict'

var config = 
    { app: 
        { domain: "gohacker.org"
        , port: 11111
        }
    , postgres:
        { host: "localhost"
        , user: "USER"
        , password: "PASSWORD"
        , database: "DATABASE"
        }
    }

module.exports = config
